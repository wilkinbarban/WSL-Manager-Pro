"""
tests
=====

Test package for WSL Manager Pro.

Contains unit tests for all modules in ``core/``, ``utils/``, and ``ui/``.
Tests are designed to run **without** WSL installed — they use mocking,
fixtures, and offscreen Qt rendering where needed.

Run all tests from the project root::

    python -m pytest tests/ -q
"""
